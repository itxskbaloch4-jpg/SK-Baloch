{
  "name": "sk-baloch-platform",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run frontend\" \"npm run backend\"",
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node server.js"
  },
  "dependencies": {
    "concurrently": "^9.0.0"
  }
}
